# AGENTS.md

## 项目工作规则

你是本项目的代码助手。每次开始任务前，请先阅读本文件，并按这里的规则执行。

## 每次任务完成后的 Git 流程

每次完成用户交代的代码任务后，请自动执行以下流程：

1. 检查当前 Git 状态：

   ```bash
   git status --short
   ```

2. 查看本次修改概况：

   ```bash
   git diff --stat
   ```

3. 查看具体修改内容，理解本次实际变更：

   ```bash
   git diff
   ```

4. 如果本次任务确实修改、新增或删除了文件，请将相关文件加入暂存区：

   ```bash
   git add .
   ```

5. 根据本次实际修改内容生成中文 commit message，并执行提交。

   commit message 格式：

   ```text
   简短标题

   修改概述：
   - 修改点 1
   - 修改点 2
   - 修改点 3
   ```

6. 提交完成后，请回复用户以下信息：

   * 是否成功提交
   * commit hash
   * 本次修改内容概述
   * 是否还有未提交文件

## 提交规则

* 不要自动执行 `git push`，除非用户明确要求。
* 如果没有任何文件变化，不要创建空提交。
* 如果测试失败、构建失败，或者修改存在明显风险，先不要提交，先告诉用户问题。
* 不要提交 `node_modules/`、`dist/`、`dist-electron/`、`.env`、日志文件等构建产物或敏感文件。
* 提交前优先确认 `.gitignore` 是否已经正确忽略无关文件。
* commit message 使用中文，简洁描述本次任务的实际修改。
* 每次提交前都要先理解修改内容，不要盲目提交。

## 项目常用命令

如果需要安装依赖：

```bash
npm install
```

如果需要启动开发环境：

```bash
npm run dev
```

如果需要构建项目：

```bash
npm run build
```

## 回复风格

* 用中文回复用户。
* 说明修改了什么，而不是只说“已完成”。
* 如果遇到错误，请给出错误原因和下一步建议。
